# 🍴 Restaurant Web Application (Flask)

Элегантный веб-сайт для ресторана с интерактивным меню, галереей блюд и системой управления контентом на базе **Flask**.

## 🌟 Основные функции
* **Интерактивное меню**: Просмотр блюд с описанием и ценами.
* **Галерея**: Визуализация интерьера и кухни ресторана.
* **Многоязычность**: Поддержка нескольких языков (если настроено в app.py).
* **PDF-меню**: Возможность просмотра/скачивания меню в формате PDF.

## 🚀 Быстрый старт

### 1. Подготовка
Активируйте ваше виртуальное окружение:
\`\`\`bash
source venv/bin/activate
\`\`\`

### 2. Установка зависимостей
\`\`\`bash
pip install flask
# Если есть дополнительные зависимости:
# pip install -r requirements.txt
\`\`\`

### 3. Запуск сайта
\`\`\`bash
python3 app.py
\`\`\`

Сайт будет доступен по адресу: **[http://127.0.0.1:5000](http://127.0.0.1:5000)**

## 📂 Организация файлов
* \`static/\` — стили CSS, скрипты JS и изображения блюд (\`img/\`).
* \`templates/\` — HTML шаблоны страниц (базовый шаблон и главная).
* \`app.py\` — основная логика сервера и маршрутизация.
* \`database.py\` — скрипты для работы с данными меню.

## 🛠 Технологии
**Python** + **Flask** + **Jinja2** + **SQLite**
EOF
